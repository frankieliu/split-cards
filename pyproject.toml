[project]
name = "card-splitter"
version = "1.0.0"
description = "Scripts to split and collage game cards into printable PDFs"
requires-python = ">=3.8"
dependencies = [
    "pillow>=10.0.0",
    "reportlab>=4.0.0",
    "pypdf>=3.0.0",
]

